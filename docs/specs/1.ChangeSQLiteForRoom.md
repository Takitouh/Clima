## Change SQLite for use Room
*Task:* 
- [X] Import dependencies and configure the project for use Room
- [X] Read and modify ClimaDatabaseHelper to change the configuration of the database for use Room
- [X] Read and modify entity classes inside _data/local/entity_ to use the annotations and correct configuration for use Room
- [X] Read and modify DAO's classes inside _data/local/dao_ and _data/local/dao/impl_ to use the annotations and correct configuration for use Room, maintain the DAO's methods, only substitute to use Room.
- [X] Run and test the project to check the correct creation of the database and correct functioning of the DAO's methods.

**Post Scriptum:**
- Removed `data/local/dao/impl/` package as Room generates implementations automatically
- DAO interfaces now use `@Dao` annotation with suspend functions for async operations
- Centralized DAO's instances and added database configuration in `AppDatabase`
- Entity classes use `@Entity`, `@PrimaryKey`, and `@ForeignKey` annotations
- Added dto `RegistroClimaConHorario` to represent the relationship between `RegistroClima` and `RegistroHorario`